describe('publishServer', () => {
	it.todo('publishes the right socket address')
	it.todo('waits to resolve until the service is published')
	it.todo('stops publishing when the server stops listening')
})
